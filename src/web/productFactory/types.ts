export interface ModelOption { id: string | number; label: string; value: string; type: string; name?: string; }
export interface Project { id: number; name: string; intro?: string; projectType?: string; type?: string; artStyle?: string; directorManual?: string; imageModel?: string; videoModel?: string; imageQuality?: string; videoRatio?: string; mode?: string; createTime?: number; }
export interface Reference { id: number; url: string; fileName: string; isPrimary: number; width?: number; height?: number; }
export interface Artifact { id: number; mediaType: "image" | "video"; slotKey: string; aspectRatio: string; version: number; url?: string; state: string; approved: number; inputChanged?: number; prompt?: string; errorReason?: string; }
export interface FactoryItem { id: number; sku: string; name: string; category?: string; description?: string; sellingPoints: string[]; attributes: Record<string, unknown>; state: string; references: Reference[]; artifacts: Artifact[]; workflow: { graph: FactoryGraph; customized: number }; }
export interface FactoryGraphNode { id: string; type: "source" | "image" | "review" | "video"; position: { x: number; y: number }; data: Record<string, any>; }
export interface FactoryGraph { productId: number; nodes: FactoryGraphNode[]; edges: Array<{ id: string; source: string; target: string }>; reviewMappings: Record<string, number | null>; viewport: { x: number; y: number; zoom: number }; customized: boolean; version: number; }
export interface Workspace { project: Project; config: any; brandReferences: Reference[]; counts: Record<string, number>; aiPolishAvailable: boolean; }
